import { ParseOpenPeriodsPipe } from './parse-open-periods.pipe';

describe('ParseOpenPeriodsPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseOpenPeriodsPipe();
    expect(pipe).toBeTruthy();
  });
});
