import { SinglePeriodPipe } from './single-period.pipe';

describe('SinglePeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new SinglePeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
